fortune | `ls /usr/games/cow* | shuf -n      1` -f `ls /usr/share/cowsay/cows/      | shuf -n 1`
"""So what is bash? 

Bash is know most commonly as, the borne-again shell. It's a shell scripting
language based off the scripting language sh.
What is a shell?
At its base, a shell is simply a macro processor that executes commands. 
A shell is software that provides an interface for an operating system's users to provide access to the kernel's services.
What's a kernel?
"The kernel is a computer program at the core of a computer\'s operating system and has complete control over everything in the system. It is the \'portion of the operating system code that is always resident in memory\', and facilitates interactions between hardware and software components."

cowsay -f fox "
